"""Evals. Separate from the package because they test it rather than belong to it.

    python -m evals.retrieval --seed 1       do the right documents come back?
    python -m evals.responses --seed 1       given that they did, does KES behave?
"""
